Create a function that collates all of the activations for a specific layer & dataset into one tensor stacked ontop of eachother.  Additionally create a datastructure that allows you to search an index of the tensor provide the token that it is then and also provide a full text snippet as well.  Additionally create a separate script that calculates the approximate amount of samples that can be processed by the SVD, O(M*N+r^2) memory bounded, where D~2304 and N is unkown, check the mmeory and to be processed given the size of a dimension of the model activation.