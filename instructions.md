Create a dataset of activations from the residual stream of Gemini 2 2b we start counting at 0, collect layers 1, layer 12, layer 20 and layer 24.  For all tokens save all activations not just the top one. Use the PILE for the dataset that we are sampling from and use activation hooks to collect them.  Don't worry about setting up the environemt I will run this on a docker container sooner.  The important thing is to randomly sample ~5.5 million tokens.  I'm aware text from the pile comes in various sizes so what I expect to be done is for each block of activations received save it within a folder and its act_{i}.pt or sample_{i}.txt.  The folders should be layer 1, layer 12, layer 20, & layer 24, and text-samples and this should I be in a folder called the PILE.  I anticpate to use more than one dataset.  Each file should be the textblock and saved in FP32.  Make sure the random seed is set each time.  